
The docker image ONLY requires the 3 files below.
Dockerfile
app.js
package.json

The following 2 are just task definitions if you want to run them from the command line.
sample-service+redis.json
sample-service-elb.json
